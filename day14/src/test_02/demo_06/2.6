1.定义一个Person类，包含一个属性：姓名(String)

​	  	定义无参、全参构造方法

​		定义get/set方法

​	2.定义main()方法，按依稀顺序编写程序：

l 定义集合List<String>，添加以下数据：

​		“王佳乐”

​		“张三丰”

​		“王思聪”

​		“张飞”

l 使用Stream流的map()方法，将每个元素封装为一个Person对象。

l 将新流中的元素提取为List集合。

l 遍历、打印集合中所有的Person对象信息。